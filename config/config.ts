// All of the values in here need to be kept in sync with the names of the actual directories and
// files.

export enum Routes {
    Home = '/',
    Projects = '/projects',
    Blog = '/blog',
}

export enum DynamicRoutes {
    ProjectPostRoute = 'project-post-name',
    BlogPostRoute = 'blog-post-name',
}
